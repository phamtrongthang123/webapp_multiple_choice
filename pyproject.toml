[project]
name = "quiz-app"
version = "0.1.0"
description = "A multiple choice quiz webapp for SAM 3D questions"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
]

[project.scripts]
quiz = "app:main"
